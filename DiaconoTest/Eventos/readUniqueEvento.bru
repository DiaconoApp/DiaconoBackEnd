meta {
  name: readUniqueEvento
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/eventos/{{eventoId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

meta {
  name: deleteEvento
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/v1/eventos/multiplos/{{eventoId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

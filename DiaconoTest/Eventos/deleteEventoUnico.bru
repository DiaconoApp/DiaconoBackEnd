meta {
  name: deleteEventoUnico
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/v1/eventos/unico/{{eventoId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

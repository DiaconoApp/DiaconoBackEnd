meta {
  name: 13 Alimentar Segunda Escala
  type: http
  seq: 13
}

patch {
  url: {{baseUrl}}/api/v1/escalas-ministerio/lider-ministerio/{{escalaEventoId2}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  content-type: application/json
}

body:json {
  [
    {
      "idExternoMembroMinisterio": "{{membroMinisterioId2}}",
      "status": "CONFIRMADO"
    }
  ]
}

tests {
  test("deve salvar segunda escala", function () {
    expect(res.getStatus()).to.equal(200);
    const body = res.getBody();
    expect(body.message).to.contain("Escala de membros do ministério atualizada com sucesso");
  });
}


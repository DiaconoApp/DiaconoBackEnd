meta {
  name: 12b Validacao Intermediaria Evento Detalhe
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/v1/eventos/{{eventoId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("detalhe do evento deve continuar acessivel no estado intermediario", function () {
    expect(res.getStatus()).to.equal(200);
    const body = res.getBody();
    expect(body.nome).to.equal(bru.getVar("nomeEvento"));
    expect(body.status).to.not.equal("CONFIRMADO");
  });
}


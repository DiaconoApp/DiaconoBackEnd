meta {
  name: removerMembroMinisterioLider
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/v1/ministerios/lider-ministerio/{{ministerioId}}/{{membroMinisterioId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
